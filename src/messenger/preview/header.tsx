import React from 'react';
import classNames from 'classnames';

const Header: React.FC = () => (
  <div className={classNames('messenger-preview-header-container')}>Header</div>
);

export default Header;
