import React from 'react';
import classNames from 'classnames';

const Footer: React.FC = () => (
  <div className={classNames('messenger-chatroom-footer-container')}>
    Footer
  </div>
);

export default Footer;
