import React from 'react';
import classNames from 'classnames';

const Chatroom: React.FC = () => (
  <div className={classNames('messenger-chatroom-container')}>chatroom</div>
);

export default Chatroom;
